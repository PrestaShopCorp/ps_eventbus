imports:
  - { resource: parameters.yml }
  - { resource: common/handler.yml }

services:
  _defaults:
    public: true

  ps_eventbus.db:
    class: Db
    factory: ["Db", "getInstance"]

  ps_eventbus:
    class: "Ps_eventbus"
    factory: ["Module", "getInstanceByName"]
    arguments:
      - "ps_eventbus"
  
  PrestaShop\Module\PsEventbus\Service\PsAccountsService:
    class: PrestaShop\Module\PsEventbus\Service\PsAccountsService
    public: true


